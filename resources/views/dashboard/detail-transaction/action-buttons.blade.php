<a class="btn btn-sm btn-outline-warning edit-transaction" data-id="{{ $transaction->username }}"
    data-name="{{ $transaction->name }}" data-transaction_id="{{ $transaction->transaction_id }}"><i
        class="icon dripicons dripicons-document-edit text-warning"></i></a>
<a class="btn btn-sm btn-outline-danger mx-2 delete-transaction" data-id="{{ $transaction->username }}"
    data-name="{{ $transaction->name }}" data-transaction_id="{{ $transaction->transaction_id }}"><i
        class="icon dripicons dripicons-trash text-danger"></i></a>
